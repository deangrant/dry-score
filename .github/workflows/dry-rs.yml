name: dry-rs

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: dry-rs-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  dry-rs:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - name: Install toolchain
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8
        with:
          toolchain: "1.94.0"

      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6

      - name: Build dry-rs
        run: cargo build --release -p dry-rs --locked

      - name: Run structural duplication analysis
        run: |
          set -euo pipefail
          report="$(
            ./target/release/dry-rs \
              . \
              --format both \
              --json-out dry-rs-report.json \
              --no-fail-on-findings
          )"
          printf '%s\n' "$report"
          if ! printf '%s\n' "$report" | grep -q 'findings=0'; then
            echo "dry-rs reported findings (expected findings=0)" >&2
            exit 1
          fi

      - name: Upload JSON report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: dry-rs-report
          path: dry-rs-report.json
          if-no-files-found: ignore

      - name: Write step summary
        if: always()
        run: |
          {
            echo "## dry-rs"
            echo
            ./target/release/dry-rs \
              . \
              --format text \
              --no-fail-on-findings
          } >> "$GITHUB_STEP_SUMMARY"
