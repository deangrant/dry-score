# Gate Contract Rule

Always-on reminder to run the correct verify tier and not relax quality gates.

## When to use

Applied automatically (`alwaysApply: true`) for all sessions in this repo.

## Usage

Agents should follow the `.mdc` after Rust edits and at plan completion.
